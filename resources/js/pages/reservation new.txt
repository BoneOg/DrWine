// Reservation.jsx
import React from "react";

const Reservation = () => {
  return (
    <div className="min-h-screen bg-black text-white flex items-center justify-center px-4 py-8">
      <div className="max-w-4xl w-full grid grid-cols-1 md:grid-cols-2 gap-10">
        {/* Left side: Calendar */}
        <div>
          <h1 className="text-4xl font-light tracking-wide font-felix mb-6">RESERVATION</h1>

          <div className="flex items-center space-x-4 mb-4">
            <select className="bg-transparent border-b border-gray-400 px-2 py-1 focus:outline-none">
              <option>April</option>
              <option>May</option>
              {/* Add more months */}
            </select>
            <select className="bg-transparent border-b border-gray-400 px-2 py-1 focus:outline-none">
              <option>18:00</option>
              <option>19:00</option>
              {/* Add more times */}
            </select>
            <select className="bg-transparent border-b border-gray-400 px-2 py-1 focus:outline-none">
              <option>2</option>
              <option>3</option>
              {/* Add more guests */}
            </select>
          </div>

          <div className="grid grid-cols-7 gap-2 text-center text-sm">
            {["MO", "TU", "WE", "TH", "FR", "SA", "SU"].map((day) => (
              <div key={day} className="text-gray-400">{day}</div>
            ))}
            {/* Sample dates */}
            {[...Array(30)].map((_, i) => (
              <div
                key={i}
                className={`py-2 rounded-full ${
                  i === 6 ? "bg-green-600 text-white" : "hover:bg-gray-700"
                }`}
              >
                {i + 1}
              </div>
            ))}
          </div>
        </div>

        {/* Right side: Form */}
        <div className="space-y-6">
          <div>
            <label className="block text-sm text-gray-400 mb-1">When</label>
            <p className="border-b border-gray-400 pb-1 text-sm">
              April 7 (Sunday), 18:00, 2 guests
            </p>
          </div>

          <div>
            <label className="block text-sm text-gray-400 mb-1">Name</label>
            <input
              type="text"
              placeholder="Linda Martin"
              className="w-full bg-transparent border-b border-gray-400 pb-1 focus:outline-none"
            />
          </div>

          <div>
            <label className="block text-sm text-gray-400 mb-1">Email</label>
            <input
              type="email"
              placeholder="lindamartin@gmail.com"
              className="w-full bg-transparent border-b border-gray-400 pb-1 focus:outline-none"
            />
          </div>

          <div>
            <label className="block text-sm text-gray-400 mb-1">Requirements</label>
            <input
              type="text"
              placeholder="Table by the window"
              className="w-full bg-transparent border-b border-gray-400 pb-1 focus:outline-none"
            />
          </div>

          <button className="bg-white text-black font-medium px-4 py-2 rounded hover:bg-gray-200 transition">
            Book a table
          </button>
        </div>
      </div>
    </div>
  );
};

export default Reservation;
